#pragma once
#include <cstddef>
#include "GLProgramUtils.h"

// Information for the construction of test models
extern float* planeVerts;
extern char const* planeTexture;
extern VertexAttribute* planeVertSpec;
extern size_t numPlaneAttrs;
extern size_t planeVertSize;
extern size_t numPlaneVerts;

extern float* cubeVerts;
extern char const* cubeTexture;
extern VertexAttribute* cubeVertSpec;
extern size_t numCubeAttrs;
extern size_t cubeVertSize;
extern size_t numCubeVerts;

extern float* shipVerts;
extern char const* shipTexture;
extern VertexAttribute* shipVertSpec;
extern size_t numShipAttrs;
extern size_t shipVertSize;
extern size_t numShipVerts;